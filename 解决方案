// 1. 修改存储路径设置
// 应该将同步文件的存储路径与配置文件路径分开，可以在配置中添加专门的同步目录配置：
type Config struct {
    // ... 其他字段 ...
    SyncStorageDir string // 同步文件的存储目录
    ConfigDir      string // 配置文件目录
}

// 2. 修改客户端视图模型
// 在创建存储服务时使用专门的同步目录：
storage, err := storage.NewFileStorage(config.SyncStorageDir, vm.logger)

// 3. 确保目录结构清晰
// - configs/：仅用于存储配置文件
// - data/：用于存储同步的文件
// - logs/：用于存储日志文件 